{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Odin Program",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}\\build\\textedit.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "Odin: Build Only"
        }
    ]
}
